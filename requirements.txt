Flask==3.1.3
reportlab==4.4.10
gunicorn==23.0.0

# Optional - improves fuzzy-match speed/quality. The app falls back to
# Python's built-in difflib automatically if this isn't installed.
rapidfuzz==3.10.1
